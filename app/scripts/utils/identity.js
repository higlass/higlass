/**
 * Identity function. Translates any variable x to x itself! Behold! It's pure
 *   magic!
 * @description  You might ask yourself. WOW! Only god him or herself must
 *   have been able to implement ths marvelous piece of software but what is
 *   it good for? In some cases we might conditionally apply certain filters
 *   mapping. In order to avoid nested conditional statements we can use the
 *   identity function as a placeholder.
 * @param   {*}  x  Something.
 * @return  {*}  Same something as before.
 */
const identity = x => x;

export default identity;
