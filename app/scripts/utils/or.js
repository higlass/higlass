/**
 * Logical or operator for convenience.
 * @param   {*}  a  Value A
 * @param   {*}  b  Value B
 * @return  {boolean}  If `true` one of the two or both values are truthy.
 */
const or = (a, b) => a || b;

export default or;
