import React from 'react';
import PIXI from 'pixi.js';
import d3 from 'd3';

export class TopBarTrack {
    constructor(props) {
        super(props);
        // this class will just receive a set of data and then render it
        //
        // the zoom, translate, position and dimensions will be properties
        // that automatically trigger the render function, hopefully this isn't too slow
        this.state = {

        };
    }


    render() {
        // called when either the state or the props change, right? 
        
    }
}
