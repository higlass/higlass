language: node_js

node_js:
  - "6"

branches:
  only:
  - master
  - develop
  - "/^v.*$/"

before_script:
  - npm install

before_deploy:
  - npm run compile

deploy:
  - provider: releases
    file: "dist.zip"
    skip_cleanup: true
    api_key: "GITHUB OAUTH TOKEN"
    on:
      tags: true
