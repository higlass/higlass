import { fetchEither } from '../app/scripts/services/tile-proxy.js';

import { expect } from 'chai';


describe('fetchEither', function() {

	it('should be a function', function() {
		expect(typeof(fetchEither)).to.eql('function??');
	});

});
